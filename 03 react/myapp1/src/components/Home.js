import React from "react";

/*
    组件的分类：组件名必须首字母大写
        1. 函数组件：
            * 就是一个普通的函数：里面必须return jsx代码(虚拟DOM)
        2.类组件：
*/

//函数组件：
function Home() {
  return (
    <div>
      <h3>函数组件</h3>
    </div>
  );
}

export default Home;
